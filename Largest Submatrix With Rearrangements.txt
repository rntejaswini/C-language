var largestSubmatrix = function(matrix) {
    let m = matrix.length;
    let n = matrix[0].length;

    // Step 1: Build heights
    for (let i = 1; i < m; i++) {
        for (let j = 0; j < n; j++) {
            if (matrix[i][j] === 1) {
                matrix[i][j] += matrix[i - 1][j];
            }
        }
    }

    let maxArea = 0;

    // Step 2 & 3
    for (let i = 0; i < m; i++) {
        // Sort row in descending order
        let row = [...matrix[i]].sort((a, b) => b - a);

        for (let j = 0; j < n; j++) {
            let height = row[j];
            let width = j + 1;
            maxArea = Math.max(maxArea, height * width);
        }
    }

    return maxArea;
};sspavan25@gmail.com



https://codelabs.developers.google.com/getting-started-google-antigravity#0




https://codelabs.developers.google.com/getting-started-google-antigravity#0
keep
Pinned
https://codelabs.developers.google.com/building-with-google-antigravity#0
keep
Pinned
https://codelabs.developers.google.com/vibe-code-with-gemini-in-aistudio#0
keep
Pinned




Dear Ma’am,

As per your instructions, I have collected the screenshots of the three sessions conducted for the Build With AI workshop.

The sessions are organized as follows:

* First batch: 2:00 PM – 3:00 PM
* Second batch: 3:30 PM – 4:30 PM
* Third batch: 5:00 PM – 6:00 PM

I have attached three separate folders containing the screenshots for each session.

Kindly review them and let me know if any further information or changes are required.

Thank you.

Regards,
Tejaswini RN
