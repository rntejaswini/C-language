#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    if (N == 1) {
        printf("2\n");
    } else if (N == 2) {
        printf("1\n");
    } else {
        printf("0\n");
    }

    return 0;
}
