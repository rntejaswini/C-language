#include <stdio.h>

int main() {
    int a = 5, b = 10, temp;

    // Swap values
    temp = a;
    a = b;
    b = temp;

    // Print result
    printf("%d %d", a, b);

    return 0;
}
